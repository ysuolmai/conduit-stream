# Placeholder component. Empty for v0.0.1 so the scaffold builds.
# When you implement discovery, this will likely just depend on IDF's `mdns`
# managed component rather than reimplementing it.
idf_component_register()
