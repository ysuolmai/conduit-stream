# mdns

Zero-config discovery so the device shows up on the LAN automatically (part of
the "be discoverable automatically" design goal). Will advertise the AirPlay and
REST services once those exist.
