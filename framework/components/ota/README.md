# ota

Over-the-air firmware updates. The `partitions.csv` table already gives you two
3 MB OTA app slots plus an `otadata` partition, so there is room to build this
out without repartitioning. This is a reused building block for Baby Arlo later.
